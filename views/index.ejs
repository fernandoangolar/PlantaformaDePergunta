<%- include('partials/header.ejs') %> 
<body>
    
    <%- include('partials/navbar.ejs') %> 

    <div class="container">
        <hr>
        <h1>Perguntar</h1>
        <hr>
        <a href="/perguntar" class="btn btn-primary">Perguntar</a>
        <br>
        <hr>
        <% perguntas.forEach(perduntas => { %>
            <br>
            <div class="card">
                <div class="card-body">
                    <h3> <%= perduntas.titulo %> </>
                </div>
                <div class="card-footer">
                    <div class="btn btn-primary">Responder</div>
                </div>
            </div>

        <% }) %>

    </div>
1
</body>
<%- include('partials/footer.ejs') %> 
